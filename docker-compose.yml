services:
    postgres:
        image: bitnami/postgresql:latest
        ports:
            - "5432:5432"
        environment:
            POSTGRESQL_USERNAME: user
            POSTGRESQL_PASSWORD: password
            POSTGRESQL_DATABASE: barber-top
        volumes:
            - postgres_data:/bitnami/postgresql
volumes:
    postgres_data:
